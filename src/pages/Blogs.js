import React from 'react';
import Header from '../shere/Header';

const Blogs = () => {
    return (
        <div>
            <Header></Header>
            <h1>this is blog page</h1>
        </div>
    );
};

export default Blogs;