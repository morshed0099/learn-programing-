import React from 'react';
import Header from '../shere/Header';

const Home = () => {
    return (
        <div>
            <Header></Header>
            <h1>home</h1>
        </div>
    );
};

export default Home;